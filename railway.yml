deploy:
  steps:
    - name: Build project
      command: composer install && yarn install && yarn product && php artisan optimize && php artisan config:cache && php artisan route:cache && php artisan view:cache 

    - name: Run tests
      command: php artisan test
